.codes
  = accordion :codes, item_options: {title_tag: "h#{heading_level}", panel_class: 'panel-warning'} do |accordion|
    = accordion.panel do |panel|
      = panel.title do
        = Code.model_name.human count: :other
        =< "(#{codes.size})"

      = panel.body do
        table.table.table-striped.table-hover.diff
          thead
            tr
              th = Image.human_attribute_name :identifier
              th = Image.human_attribute_name :url
              th = Image.human_attribute_name :created_by
              th = Image.human_attribute_name :created_at
              th = Image.human_attribute_name :updated_at

          tbody
            - codes.each do |code|
              == render 'code', code: code
