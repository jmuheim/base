.pastables
  == render 'images/images', images: images, heading_level: heading_level ||= 2 if can?(:index, Image) && images.any?
  == render 'codes/codes',   codes:  codes,  heading_level: heading_level ||= 2 if can?(:index, Code)  && codes.any?